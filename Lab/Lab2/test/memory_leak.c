#include<stdio.h>
#include<stdlib.h>

int main(){
    // char *a = (char *)malloc(sizeof(char) * 20) ;
    // char *b = (char *)malloc(sizeof(char) * 30) ;

    // a[0] = 'a' ;
    // free(b) ; 
    int a[5] = {13, 24, 35, 46, 57};
    printf("%d\n", a[5]);


    return 0 ;
}